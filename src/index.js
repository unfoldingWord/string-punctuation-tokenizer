import {
  tokenize,
  tokenizeWithPunctuation,
  occurrenceInString,
  occurrencesInString,
} from './tokenizers';

export default {
  tokenize,
  tokenizeWithPunctuation,
  occurrenceInString,
  occurrencesInString,
};
